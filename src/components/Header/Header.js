import React from "react";
import styles from "./Header.module.css";

const Header = () => (
    <div className={styles.container}>
        <p className={styles.caps}>Path Finding Visualizer</p>
    </div>
);

export default Header;
